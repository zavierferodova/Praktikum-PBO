package second;

import first.PublicModifier;

public class PublicDemo {
    public static void main(String[] args) {
        PublicModifier pmod = new PublicModifier();
        pmod.kali();
        pmod.tambah();
        pmod.kurang();
        pmod.bagi();
        pmod.rataRata();    
    }
}
